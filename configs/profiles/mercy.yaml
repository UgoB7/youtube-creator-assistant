profile:
  id: mercy
  display_name: Motion-Assisted Workflow
  visual_input_mode: image_or_video

paths:
  runtime_root: ../../runtime/mercy
  outputs_dir: ../../runtime/mercy/outputs
  incoming_dir: ../../runtime/mercy/incoming
  images_dir: ../../runtime/mercy/images
  logs_dir: ../../runtime/mercy/logs
  psalms_dir: ../../assets/audio/psalms
  gospel_dir: ../../assets/audio/gospel

workflow:
  fps: 30
  target_duration_tc: "3:33:32:20"
  trim_first_audio_seconds: 3.2
  include_gospel: true
  max_head_items: 3
  max_reference_summaries: 5

thumbnail:
  max_bytes: 2097152
  target_bytes: 1800000
  suffix: "_yt"

openai:
  model: gpt-5.2-2025-12-11
  title_examples_input: ""
  devotional_examples_input: ""

description:
  variant: mercy_legacy
  audio_explanation_count: 5

replicate:
  enabled: true
  candidate_count: 10
  prompt_style: mercy_legacy
  prompt_seed_path: ../../assets/prompts/shepherd_prompts.txt
  image_model: black-forest-labs/flux-2-max
  image_payload_style: flux
  image_output_format: png
  image_size: "2 MP"
  image_aspect_ratio: "16:9"
  image_output_quality: 100
  image_safety_tolerance: 5
  video_model: bytedance/seedance-1.5-pro
  video_fps: 24
  video_duration: 12
  video_resolution: 1080p
  video_aspect_ratio: "16:9"
  video_camera_fixed: true
  video_generate_audio: false
  video_prompt: "no camera movement. no talking. mouth closed. "

web:
  host: 127.0.0.1
  port: 5002
  debug: true

render:
  enabled: true
  backend: resolve
  timeline_prefix: mercy
  timeline_mode: existing_only
  append_mode: sequential_exact
  clean_media_pool_imports: true
  media_pool_folder_name: YCA Imports
  import_only_required_media: true
  video_mode: image_or_video
  audio_strategy: cut_to_duration
  video_strategy: loop_to_duration
  video_timing_source: metadata_first
  image_strategy: resolve_still_duration
  do_render: false
  render_dir: ../../runtime/mercy/renders
  width: 1920
  height: 1080
